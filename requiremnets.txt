# Core Streamlit + data handling
streamlit         # latest stable versions as of late 2025
pandas
numpy
openpyxl           # needed for Excel reading/writing
xlrd                # sometimes needed for older .xls files

# Date/time and regex
python-dateutil
pytz

# Temporary files, shutil, etc. (usually already in stdlib, but good to note dependencies)
# No extra packages needed for tempfile, shutil, os, re, io, collections

# Optional but recommended for better performance / features
matplotlib        # if you ever add charts in the future
xlsxwriter          # alternative Excel engine (faster in some cases)



# numpy
